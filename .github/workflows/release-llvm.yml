name: Build and Release LLVM

on:
  workflow_dispatch:
    inputs:
      release_tag:
        description: "Release tag (e.g., v20.1.4)"
        required: true
        type: string

permissions:
  contents: write

jobs:
  create-release:
    runs-on: ubuntu-22.04
    outputs:
      upload_url: ${{ steps.create_release.outputs.upload_url }}
    steps:
      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ inputs.release_tag }}
          release_name: LLVM Release ${{ inputs.release_tag }}
          draft: false
          prerelease: false

  # linux-release:
  #   needs: create-release
  #   # Building on a older Ubuntu to build with a more ancient libc to support more platforms
  #   runs-on: ubuntu-22.04
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #     # -------------------------------------------------------------------------------
  #     - name: Install dependencies
  #       run: |
  #         sudo apt-get update
  #         sudo apt-get install -y cmake ninja-build xz-utils git
  #     # -------------------------------------------------------------------------------
  #     - name: Build and package LLVM
  #       run: bash build_llvm.sh
  #     # -------------------------------------------------------------------------------
  #     - name: Upload Release Artifact
  #       uses: actions/upload-release-asset@v1
  #       env:
  #         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  #       with:
  #         upload_url: ${{ needs.create-release.outputs.upload_url }}
  #         asset_path: .llvm/linux-x64.tar.xz
  #         asset_name: llvm-linux-x64.tar.xz
  #         asset_content_type: application/x-xz

  # macos-release:
  #   needs: create-release
  #   runs-on: macos-latest
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #     # -------------------------------------------------------------------------------
  #     - name: Install dependencies
  #       run: |
  #         brew install cmake ninja xz
  #     # -------------------------------------------------------------------------------
  #     - name: Build and package LLVM
  #       run: bash build_llvm.sh
  #     # -------------------------------------------------------------------------------
  #     - name: Upload Release Artifact
  #       uses: actions/upload-release-asset@v1
  #       env:
  #         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  #       with:
  #         upload_url: ${{ needs.create-release.outputs.upload_url }}
  #         asset_path: .llvm/macos-x64.tar.xz
  #         asset_name: llvm-macos-x64.tar.xz
  #         asset_content_type: application/x-xz

  windows-release:
    needs: create-release
    runs-on: [
      '@id:llvm-win-release-job-${{ github.run_id }}-${{ github.run_attempt }}',
      '@image-family:tracel-ci-windows-2022',
      '@keep-alive:true',
      '@machine-type:c2-standard-16',
      '@os:windows',
      '@zone:northamerica-northeast1-b'
      ]
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      # -------------------------------------------------------------------------------
      - name: Build and package LLVM
        shell: pwsh
        run: |
          ./build_llvm.ps1
      # -------------------------------------------------------------------------------
      - name: Upload Release Artifact
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ needs.create-release.outputs.upload_url }}
          asset_path: .llvm/windows-x64.tar.xz
          asset_name: llvm-windows-x64.tar.xz
          asset_content_type: application/x-xz
